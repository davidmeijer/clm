# PARAS

Fast implementation of PARAS.

## Installing HMMER2 on macOS Arm64

Use Rosetta to install the xf86_64 version of HMMER2:

```bash
conda activate paras-fast
conda config --env --set subdir osx-64
conda install hmmer2
```
